# Data Protection Impact Assessment — Sensitive Wellness Data

**Document scope:** Internal compliance artifact. Regulator-facing only. Not for public distribution.

---

## 1. Document Control

| Field | Value |
|---|---|
| Document title | Data Protection Impact Assessment — Sensitive Wellness Data |
| Version | 2.3 |
| Effective date | 2026-05-24 |
| Last amended | 2026-08-10 (MAINT-401 — see §9 change log) |
| Next scheduled review | 2027-05-24 |
| Owner | Palouse Labs LLC (sole proprietor) — responsibility is non-delegable |
| Application | Being (Stoic Mindfulness wellness app) |
| Triggering statutes | Tex. Bus. & Com. Code §541.105(a) (TDPSA), C.R.S. §6-1-1309 (CPA), Va. Code §59.1-580 (VCDPA), Conn. Pub. Act 22-15 §6 (CTDPA) |
| Surfaced by | INFRA-83 regulatory re-scope audit (2026-05-24) |

**Material change trigger.** This assessment will be revised before, not after, any of the following:

- A new category of sensitive wellness data is collected, derived, or inferred.
- A new third-party processor receives any sensitive wellness data.
- The local-first architecture changes — for example, sensitive wellness data becomes server-decryptable, or a non-user party gains decryption keys.
- Being begins serving a new jurisdiction whose privacy law differs materially from those listed above.

---

## 2. Scope

**Processing activities covered by this DPIA:**

- Local-first capture, storage, and display of sensitive wellness data on a user's own device.
- Optional end-to-end encrypted backup to Supabase. The user holds the encryption key; Supabase has no decryption capability.
- Subscription billing metadata processed via Stripe. Stripe receives payment instruments and transaction state; Stripe does not receive any wellness content.
- Product analytics (PostHog) — severity-bucket aggregates and feature-engagement events, transmitted only when the user has granted analytics consent (PostHog is not initialized without it; universal opt-out suppresses transmission). PostHog receives no raw screening scores, no Q9 value, no journal content, and no quasi-identifiers. EU data residency (Frankfurt).
- Crisis-detection telemetry (Supabase `analytics_events`) — a `crisis_detected` event recorded to Being's own first-party table when a PHQ-9 total ≥20, a non-zero PHQ-9 Q9, or a GAD-7 total ≥15 is detected. Recorded **regardless of analytics consent** under GDPR Art. 6(1)(d)/9(2)(c) vital interests (see §4 and the standalone `lia-crisis-telemetry.md`). Payload: `trigger_type` (category — `phq9_suicidal_ideation` / `phq9_severe_score` / `phq9_moderate_severe_score` / `gad7_severe_score`), `severity_bucket`, `intervention_surfaced`, `assessment_type`. No raw score, no Q9 value, no device identifier; `session_id` is a daily-rotated anonymous token that cannot be joined to a user identity.
- Error and crash telemetry via Sentry — payload scrubbing in place per §7. Sentry also receives release-health session data (session start/end, duration, crash status) carrying a per-install identifier (not linked to identity or wellness data); see §9 v1.7 for the retrospective finding.
- Crisis-monitoring operational egress — the `crisis-detection-alerting` edge function emails the founder on a breach via **Resend** (aggregate, non-personal alert payload; see §9 v1.4) and fires a PII-free liveness heartbeat to **healthchecks.io** as an external dead-man's-switch (INFRA-264; an opaque HTTP ping carrying no user data, no wellness data, and no identifier — see §9 v1.6). Neither receives sensitive wellness data.

**Out of scope.** Being does not engage in advertising, data sale, third-party sharing for non-service purposes, profiling for automated decisions, or training of generative models. See `docs/legal/privacy-policy.md` §5 for the public confirmation.

**Architecture summary.** Local-first by design. The user's device is the primary system of record; optional cloud backup contains only opaque encrypted blobs. The full technical specification is in `docs/security/security-architecture.md`.

---

## 3. Data Categories and Sensitivity Classification

The following categories are classified as **sensitive personal information** under one or more of the triggering statutes. Mental and physical health condition data is enumerated as "sensitive data" in each statute's definitional section (see §11).

| Category | Examples | Classified sensitive under |
|---|---|---|
| Wellness screening responses — depression module | PHQ-9 item responses, total score, Q9 self-harm indicator | TDPSA, CPA, VCDPA, CTDPA |
| Wellness screening responses — anxiety module | GAD-7 item responses, total score | TDPSA, CPA, VCDPA, CTDPA |
| Mood check-in records | Numeric mood rating, optional context tag | TDPSA, CPA, VCDPA, CTDPA |
| Reflective writing | Journal entries from Stoic Mindfulness practices | TDPSA, CPA, VCDPA, CTDPA |
| Crisis safety plan content | Personal warning signs, coping strategies, support contacts | TDPSA, CPA, VCDPA, CTDPA |
| Consent records for sensitive-data processing | Acknowledgment timestamps for explicit consent under GDPR Art. 9(2)(a) where applicable, and state-law sensitive-data consent | Derivative — sensitive because the consent record itself confirms a user's participation in mental wellness self-screening |
| Subscription status and transaction history | Active plan, billing events, entitlement status for crisis-resource features | Derivative — sensitive because correlation confirms ongoing engagement with mental wellness self-monitoring services |

Where this DPIA refers to "sensitive wellness data" without qualification, all seven categories are intended.

**Local crisis-intervention records (added v1.8, MAINT-334; extended v1.9, DEBUG-355).** This is a coverage note on the existing categories above, not an eighth category. Prior to DEBUG-305 the app wrote a separate `crisis_intervention_<assessmentId>` record to unencrypted `AsyncStorage` on each detection. That write has been removed at source, and the record is not re-created: the derived crisis signal now lives only inside the depression- and anxiety-module screening categories already enumerated above, and the durable crisis audit trail is the off-device `crisis_detected` event described in §2 and §4.

**Correction (v1.9, DEBUG-355):** as written at v1.8 the paragraph above was **not yet accurate**, and the inaccuracy was in the direction of understating local retention. Two further local crisis-path writes existed at that time, neither enumerated anywhere in this assessment:

- `critical_log_<epoch>` — written to **SecureStore** by `ProductionLogger.storeCriticalEntry` for every log entry at ERROR level *or* in the CRISIS category. Because SecureStore has no enumerate API and the key carried a timestamp, the record matched no fixed erasure manifest and no sweepable prefix: it was **unerasable by construction**. Account deletion could not remove it and no launch-time sweeper could have, since nothing could learn the key names. The entry was written in cleartext in every real configuration — the surrounding encryption tier's enable path had zero callers, so the "encrypt, else store plaintext" branch always took the plaintext arm. The write has been deleted at source rather than re-keyed; it was write-only data no code ever read back.
- `audit_log_<epoch>` — written to **AsyncStorage** by `SecureStorageService.logStorageAccess` on every **failed** storage operation. It carries storage-access metadata (operation type, storage key, tier, sensitivity level, timing), not wellness content. This one was unerasable only **by omission**: the keys enumerate, and `audit_log_` was simply absent from the erasure sweep. It has been added, which reaches records already written on shipped installs with no migration. *(MAINT-378: until that change this record was also written on every `crisis_tier` operation, and so recorded that a crisis-tier record existed and when. The crisis tier has been deleted as dead code — see v2.0 below — so the failure arm is now the only writer, and no new record of that kind can be produced.)*

The residual for the first of these is recorded in §8; the controls are §7 items 17–18. A **third** unenumerated write, `storage_metadata_index`, was live throughout both of the corrections above and is closed by DEBUG-381 under control 19 — see the correction and the positive key enumeration immediately below.

**Correction (DEBUG-381).** The paragraph below asserted that exactly one locally persisted crisis-path artifact remained. That was **false when written**, and it is the third consecutive version of this section to assert completeness and be falsified: v1.8 claimed the local footprint was closed by DEBUG-305, v1.9 corrected that and claimed it was closed by DEBUG-355, and both were wrong because `storage_metadata_index` was live throughout. Each was found the same way — by enumerating the whole store after erasure while testing the *previous* fix — never by review.

The recurring failure mode is omission, so this section now carries a **positive enumeration** (below) instead of a fourth per-defect assurance. A negative claim of the form "nothing else remains" has a 0-for-3 record here and is not independently checkable; a list of every persisted key on the wellness/crisis path is. The AsyncStorage half of that list is machine-verified: `crisisRecordErasure.privacy.test.ts` reads the same `SWEPT_ASYNC_PREFIXES` / `SWEPT_EXACT_KEYS` constants production filters on and asserts nothing escapes them.

**Every persisted key on the wellness/crisis path.**

| Key / pattern | Store | Written by | Encrypted? | Erasure mechanism |
|---|---|---|---|---|
| `crisis_async_<id>` | AsyncStorage | *no writer since MAINT-378* — the tier's only writer (`storeCrisisData`) is deleted; the prefix is retained in the sweep as a defensive floor for shipped installs | AES-256-GCM | `SWEPT_ASYNC_PREFIXES` |
| `assessment_async_<id>` | AsyncStorage | `storeAssessmentData` | AES-256-GCM | `SWEPT_ASYNC_PREFIXES` |
| `wellness_async_<id>` | AsyncStorage | `storeWellnessBlob` | AES-256-GCM | `SWEPT_ASYNC_PREFIXES` |
| `wellness_migrated:<id>` | AsyncStorage | migration marker | no (version marker only) | `SWEPT_ASYNC_PREFIXES` |
| `audit_log_<epoch>` | AsyncStorage | `logStorageAccess` | **no** | `SWEPT_ASYNC_PREFIXES` (control 18) |
| `storage_metadata_index` | AsyncStorage | `storeMetadata` / `deleteMetadata` (reached only via `storeAssessmentData` since MAINT-378) | **no** | `SWEPT_EXACT_KEYS` **+ cache clear** (control 19) |
| `@being/supabase/crisis_analytics_queue` | AsyncStorage | `trackCrisisDetection` | no (by design — must stay flushable) | `SWEPT_EXACT_KEYS` (control 15) |
| `crisis_intervention_*`, `assessment_audit_trail` | AsyncStorage | *legacy, no longer written* | no | launch sweeper + sweep (control 16) |
| `WELLNESS_SECURE_STORE_KEYS` (6 keys) | SecureStore | various | AES-256-GCM | explicit manifest delete |
| `ERASURE_EXCLUDED_SECURE_STORE_KEYS` (5 keys) | SecureStore | consent / identity | — | **deliberately retained** (lawful-basis evidence + identity anchor) |

The SecureStore half **cannot** be machine-enumerated — the platform offers no enumerate API, which is precisely why `critical_log_*` was irreducible under DEBUG-355 §8 and had to be fixed by deleting the write rather than by sweeping. That half is a manual list and should be re-derived, not trusted, whenever a new SecureStore key is introduced.

**Residual this does not close.** DEBUG-381 makes `storage_metadata_index` *erasable*; it does not make it *confidential*. It remains an unencrypted AsyncStorage record naming stored-record ids, their tier, their sensitivity level and their access timing, present for the life of the install until erasure. Since MAINT-378 deleted the crisis tier, the tiers that can populate it are `assessment_tier` and below — which narrows what it discloses without changing that it discloses it. Encrypting or minimising it is a separate decision, not taken here.

---

The pending-upload buffer, enumerated rather than denied: `@being/supabase/crisis_analytics_queue` is a pending-upload buffer written to plain `AsyncStorage` at fire-time so that a first-run or offline detection is not silently dropped before it can reach the server (`SupabaseService.ts` → `trackCrisisDetection`). It holds only the same bucketed fields the transmitted event carries — `trigger_type`, `severity_bucket`, `intervention_surfaced`, `assessment_type`, plus the daily-rotated anonymous `session_id` and an enqueue timestamp. It carries no raw score, no Q9 value, no free text, and no device identifier. It is transient by design: entries are removed on successful flush, and any unflushed remainder is deleted on account erasure under control 15. Because it is a pending-upload buffer for an already-enumerated off-device event rather than a durable local record of its own, it does not add a category to the table above.

---

## 4. Purposes of Processing

Each of TDPSA, CPA, VCDPA, and CTDPA requires opt-in consent for processing of sensitive personal information. That state-law sensitive-data consent is the operative basis for each purpose involving categories enumerated in §3; tracking is described in the consent-records entry of §3. The "GDPR Art. 6 equivalent" column below maps each purpose to the corresponding GDPR lawful basis for cross-jurisdictional clarity — it is the equivalent, not a substitute for state-law consent.

| Purpose | Categories used | GDPR Art. 6 equivalent (where applicable) |
|---|---|---|
| Wellness self-monitoring — present a user's own screening history and trends to themselves | Wellness screening responses, mood check-ins, journal | User consent; service provision |
| Personal insights — derive aggregated, on-device patterns to support self-reflection | Wellness screening responses, mood check-ins | User consent; service provision |
| Crisis-resource access — surface 988 and personal crisis contacts when self-harm indicators are present | Wellness screening responses (PHQ-9 Q9), crisis safety plan content | Vital interests; user safety |
| Crisis-detection telemetry — record an aggregate, PII-free `crisis_detected` event to first-party Supabase `analytics_events` when a crisis threshold is met, for operational safety monitoring (verify interventions surface correctly) | Wellness screening responses (category only — trigger type + severity bucket; not raw score or Q9 value) | GDPR Art. 6(1)(d) / 9(2)(c) vital interests; user safety. Recorded without analytics consent; universal opt-out does **not** suppress it. See `lia-crisis-telemetry.md`. |
| Subscription entitlement — confirm whether a user's paid plan is active for crisis-feature access | Subscription status and transaction history | Contract performance |
| Security and operational integrity — error monitoring, RLS-based isolation, audit logging on subscription events | Subscription transaction history; scrubbed error telemetry | Legitimate interests; legal obligation (breach detection) |

Being **does not** use any of the categories listed in §3 for advertising, data sale, profiling for automated decisions about a user, training of generative models, or any purpose not enumerated above.

---

## 5. Necessity and Proportionality

For each category in §3, Being collects the minimum needed for the stated purpose.

- **PHQ-9 (nine items)** is the standard short-form depression screener. A shorter instrument (PHQ-2, PHQ-4) was considered and rejected: it omits Q9 (self-harm indicator), which is the technical basis of Being's crisis-resource routing. PHQ-9 is the minimum that preserves the crisis-detection capability.
- **GAD-7 (seven items)** is the standard short-form anxiety screener. The GAD-2 sub-score lacks the granularity needed for the trend visualizations that motivate sustained self-monitoring. GAD-7 is the minimum that preserves the user-facing insight.
- **Mood check-ins** capture a single numeric rating and an optional context tag. This is the minimum signal needed to plot a mood trend.
- **Journal entries** are user-authored reflective writing. The user controls content and length. No content analysis, classification, or machine-learning inference is performed by Being or by any third party.
- **Crisis safety plans** are user-authored. They are collected because the alternative — no safety plan — materially reduces the effectiveness of the crisis-resource purpose.
- **Consent records** are collected because GDPR Art. 9(2)(a) requires them where it applies, and because state-law sensitive-data consent is a best-practice control. The record contains a timestamp and the acknowledged scope only — no derivative information.
- **Subscription metadata** is the minimum needed to enforce paid-tier entitlements; raw payment instruments are processed by Stripe, not by Being.

Local-first storage is the primary proportionality control: sensitive wellness data does not leave the user's device unless the user opts into encrypted backup. When opted in, cloud backup transmits only opaque encrypted blobs.

---

## 6. Risk Assessment

Scored on a qualitative 3×3 likelihood × impact matrix (Low / Med / High). "Pre-mitigation" assumes only baseline operating-system controls (no Being-specific protections). "Post-mitigation" reflects the controls inventoried in §7.

| # | Threat scenario | Pre-mitigation (Likelihood × Impact) | Post-mitigation (Likelihood × Impact) | Residual |
|---|---|---|---|---|
| 1 | Re-identification of an individual from in-app analytics aggregates | Med × Med | Low × Low | **Low** |
| 2 | Unauthorized access to sensitive wellness data via a lost, stolen, or shared device | High × High | Low × High | **Low** (likelihood) — see rationale |
| 3 | Compromise of optional cloud backup at the Supabase storage layer | Low × High | Low × Low | **Low** |
| 4 | Leakage of sensitive wellness data through error or crash telemetry (Sentry) | Med × Med | Low × Low | **Low** |

### 6.1 Rationale per scenario

**Scenario 1 — Re-identification from analytics aggregates.** After the INFRA-214 consolidation the analytics pipeline has two sinks, neither of which receives raw screening scores, Q9 values, journal content, location, device fingerprint, or any other quasi-identifier. **PostHog** (product analytics, consent-gated) carries severity buckets and feature-engagement events under EU data residency (Frankfurt). **Supabase `analytics_events`** (crisis-detection telemetry, vital-interests basis) carries only `trigger_type` (category), `severity_bucket`, `intervention_surfaced`, and `assessment_type`; its `session_id` is a daily-rotated anonymous token generated at app launch that does not persist across calendar days and cannot be joined to a user identity, and a DB CHECK constraint enforces that format. RLS isolates rows by an opaque identifier (`docs/security/supabase-rls-verification.md`, Analytics Events Table). **k-anonymity and differential privacy are NOT claimed** — the dead custom-API engine that notionally provided them was deleted in INFRA-214, and at pre-launch scale such thresholds are not operationally meaningful. Re-identification is instead managed by severity-bucketing, absence of quasi-identifiers, daily `session_id` rotation, and first-party RLS — sufficient to keep re-identification not realistically achievable. Post-mitigation residual: Low.

**Scenario 2 — Unauthorized device access.** This is the highest-impact scenario because device loss is common. Mitigations: sensitive wellness data is encrypted at rest using AES-256-GCM with keys held in operating-system-protected stores (iOS Keychain configured without iCloud sync; Android Keystore with StrongBox where available). Sensitive views require operating-system-level device unlock. See `docs/security/security-architecture.md` §1 (Encryption Methods for Local Storage) and §3 (Biometric Authentication Implementation). Likelihood that an attacker with physical possession of the device also defeats the device-unlock layer and the OS keystore is Low. Impact, were they to succeed, remains High because the content is sensitive — but the chain of controls makes successful exploitation a Low-likelihood outcome. The user remains the last line of defense by setting and protecting their device passcode/biometric.

**Scenario 3 — Cloud backup breach.** Optional cloud backups are stored as opaque encrypted blobs in the `encrypted_backups` table. The decryption key never leaves the user's device. Supabase Row-Level Security prevents cross-user access at the database layer (see `docs/security/supabase-rls-verification.md`, Encrypted Backups Table within RLS Policy Analysis). Even in the event of a full Supabase compromise, attacker yield is encrypted blobs without accompanying keys. Residual: Low.

**Scenario 4 — Sentry telemetry leakage.** React Native error boundaries can capture local variable state into error payloads. Production mitigations: a `beforeSend` hook in `app/src/core/services/logging/ExternalErrorReporter.ts` (`beforeSendHook`, line 541) sanitizes every event before transmission — a crisis-content scan over named content-bearing surfaces (`collectContentText`, line 626) that drops a matching event **wholesale**, then an allowlist rebuild (`applyAllowlist`, line 695), then a dedicated wellness-data scrubbing path (`scrubSensitiveData`, line 770); sensitive-data variables are never passed into log lines or exception messages by convention. Stack frames are exempt from the content scan and coarsened instead — see controls 10a/10b in §7 for the ruling and its prospective status. Defense-in-depth for the development environment: the dev-environment Sentry DSN is empty (configured in `app/src/core/config/env.ts`), so a developer's local debugging cannot transmit at all — this is a developer-side safeguard, not a production control. Residual: Low. This scenario warrants explicit attention at every annual review since the React Native error-boundary surface area can grow with new features.

Retrospective correction (INFRA-295, v1.7): a naming mismatch (`autoSessionTracking` vs. the SDK's actual `enableAutoSessionTracking`) meant release-health session envelopes — which carry no wellness content but, absent an explicit `Sentry.setUser` call, do carry a per-install identifier via the native layer's `did` fallback — were most likely transmitted from every production build to date. Note also that session envelopes are envelope *session items*, not events, so the `beforeSend` mitigation credited above does not apply to them; their privacy posture rests on the fixed session schema having no field for user content. See §9 v1.7 for the finding and remediation.

---

## 7. Mitigation Measures

This table inventories the controls in place and cites the authoritative reference for each. Algorithm parameters (key sizes, iteration counts, exact protocol versions) are intentionally not duplicated here — refer to the cited source so that this DPIA remains coherent if those parameters are tightened over time.

| # | Control | Authoritative reference | Section |
|---|---|---|---|
| 1 | AES-256-GCM at rest for sensitive wellness data | `docs/security/security-architecture.md` | §1 Encryption Methods for Local Storage |
| 2 | Operating-system-protected key storage (iOS Keychain without iCloud sync; Android Keystore / StrongBox) | `docs/security/security-architecture.md` | §1 Encryption Methods for Local Storage |
| 3 | Biometric / passcode authentication for sensitive data views | `docs/security/security-architecture.md` | §3 Biometric Authentication Implementation |
| 4 | Auto-timeout and session lock | `docs/security/security-architecture.md` | §4 Auto-Timeout and Session Management |
| 5 | TLS 1.2+ in transit | `docs/legal/privacy-policy.md` | §4.3 Security Measures |
| 6 | Row-Level Security on all Supabase tables containing user data — keyed on `auth.uid()` and **runtime-enforced** (INFRA-260). The prior `device_id`-GUC policies were SQL-editor-verified only and inert on the runtime path; INFRA-260 re-verified isolation via the authenticated API path with `WITH CHECK` on writes. | `docs/security/supabase-rls-verification.md` v2.0 | RLS Policy Analysis (all five tables) + Runtime Verification |
| 7 | End-to-end encrypted backup blobs (decryption key never leaves the device) | `docs/security/supabase-rls-verification.md` | RLS Policy Analysis — Encrypted Backups Table |
| 8 | Audit logging on subscription events | `docs/security/supabase-rls-verification.md` | RLS Policy Analysis — Subscription Events Table |
| 9 | Analytics severity-bucketing (no raw scores transmitted) | `docs/security/supabase-rls-verification.md` | RLS Policy Analysis — Analytics Events Table |
| 10 | Sentry `beforeSend` payload scrubbing applied to every transmitted event; stack-frame slots structurally exempted from the content scan and coarsened instead | `app/src/core/services/logging/ExternalErrorReporter.ts` | `beforeSendHook` (line 541); content scan `collectContentText` (line 626); allowlist frame rebuild `applyAllowlist` (line 695); wellness-data scrubbing `scrubSensitiveData` (line 770); frame coarsening `sanitizeFilename` (line 856) and `sanitizeFrameFunction` (line 887) |
| 10a | **Ruling (MAINT-401) — a crisis path segment in a stack frame is coarsened, not transmitted and not dropped.** `exception.values[].stacktrace` is exempt from the crisis-content scan (DEBUG-338): a frame is structural metadata, and dropping an event over one would destroy the crash-triage purpose that legitimises transmitting it. Candidate shape "bare exemption, no coarsening" was **considered and declined** — it would recreate, through a slot nothing coarsens, the same sensitive-surface signal that `sanitizeScreenName` / `isSensitiveRoute` exist to close. Instead `frames[].filename` is coarsened **segment-wise** and `frames[].function` **whole-token** against the shared `isSensitiveRoute` keyword set, so the two paths provably cannot drift; non-sensitive segments survive verbatim so triage value is retained. The exemption is scoped **structurally to the frame slots** (both coarsening functions are reachable only from the frame map and `sanitizeStack`), never as a "strip path-shaped substrings before scanning" heuristic — such a heuristic would exempt path-shaped *content* such as `crisis_alerts/insert score=21` wholesale, which `scrubSensitiveData` cannot recover. **Compensating control:** `applyAllowlist` does not pass frames through; it rebuilds each as exactly `{filename, function, lineno, colno}`, dropping `context_line`, `pre_context`, `post_context`, `abs_path`, `module` and `vars` — the fields that can carry real source text. | `app/__tests__/privacy/stackFrameExemption.contract.test.ts` | Coarsening, verbatim-survival, slot-scoping and the four-scalar rebuild are each pinned by a named test |
| 10b | **Status of control 10a: PROSPECTIVE hardening, not a fix to a live exposure — recorded as such deliberately.** On every shipping build no real source path reaches `beforeSend`: `@sentry/react-native` 7.11.0's default `createReactNativeRewriteFrames()` hard-assigns `filename` to `app:///main.jsbundle` under Expo and deletes `abs_path`, running inside `_prepareEvent` which resolves *before* `processBeforeSend`; the only integration that restores real paths, `debugSymbolicatorIntegration()`, is `__DEV__`-gated, and the development DSN is empty. Release bundles are minified, so `function` is mangled. No Sentry source-map or debug-id upload is configured, so paths cannot be re-resolved server-side either. **Three falsifiers would make 10a a live control, and any of them requires re-review before shipping:** (i) enabling Sentry source-map / debug-id upload; (ii) a non-empty DSN in any `__DEV__` build — note `debugSymbolicatorIntegration` registers *after* the rewriter and overwrites its output, so the empty dev DSN is load-bearing, not defense-in-depth; (iii) leaving Expo, or `isExpo()` returning false, which is a runtime global check rather than a build guarantee. **Separately noted, not fixed:** `scrubSensitiveData`'s base64 heuristic `(?:[A-Za-z0-9+/]{4}){10,}` includes `/` in its class and therefore blanket-redacts any path with a ≥40-character alphanumeric-plus-slash run, while leaving shorter paths intact — an accidental length-dependent behaviour that control 10a replaces with a decided one. | `app/src/core/services/logging/ExternalErrorReporter.ts`; `app/eas.json`; `app/.env.development` | Pinned SDK version 7.11.0. Assessment holds only for that version and the Expo/CNG build shape |
| 11 | Sentry disabled in development environment via empty DSN (defense-in-depth) | `app/src/core/config/env.ts` | `EXPO_PUBLIC_SENTRY_DSN` handling |
| 12 | Secure data export and complete data deletion — on-device wipe plus, post-INFRA-260, server-side erasure: the `delete-account` edge function hard-deletes the user's `auth.users` row and the FK `ON DELETE CASCADE` removes every `auth.uid()`-keyed row (backups, analytics, subscriptions). | `docs/security/security-architecture.md`; `supabase/functions/delete-account` | §5 Secure Export Mechanisms; §6 Complete Data Deletion |
| 13 | Crisis-detection telemetry: PII-free payload (no raw score, no Q9 value, no device identifier; daily-rotated anonymous `session_id`), enforced by the emitter's explicit allow-list + a DB CHECK constraint | `docs/legal/lia-crisis-telemetry.md` | §4 Safeguards |
| 14 | Crisis-detection telemetry: durable lossless capture (enqueued at fire-time to a pending-upload buffer, reconciled to the anonymous user on connectivity) so a first-run/offline detection is recorded, not silently dropped. **Corrected v1.8 (MAINT-334):** v1.2–v1.7 additionally credited a "separate mandatory on-device audit log" here. DEBUG-305 removed the function that wrote it (`logCrisisIntervention`); no such local audit exists and none is credited as a control. The pending-upload buffer is the only local artifact, and its erasure is control 15. | `app/src/core/services/supabase/SupabaseService.ts` | `trackCrisisDetection` / `flushCrisisAnalytics` |
| 15 | Erasure coverage for the crisis-telemetry buffer: `clearAllWellnessData` sweeps the `crisis_async_` / `assessment_async_` / `wellness_async_` / `wellness_migrated:` prefix families, plus an auditable exact-name exception list (`SWEPT_EXACT_KEYS`). `@being/supabase/crisis_analytics_queue` is named on that list rather than renamed to a swept prefix, because renaming would strand events already queued on existing installs. It is swept on erasure but deliberately **not** purged at launch — it must survive ordinary restarts to flush. On account deletion this ordering is load-bearing: `deleteAccountAndWipe` erases the server *before* wiping locally, so a surviving queue would flush on next launch and re-create crisis rows for an account the user had deleted. The exception list is pinned by a privacy test that reads the same constant, so it cannot silently grow a hole. | `app/src/core/services/security/SecureStorageService.ts`; `app/__tests__/.../crisisRecordErasure.privacy.test.ts` | `SWEPT_EXACT_KEYS` / `clearAllWellnessData` |
| 16 | Remediation of pre-fix local records: `legacyPlaintextRecordSweeper` runs at launch from `App.tsx` before render and **deletes** any residual `crisis_intervention_*` record and the legacy bare `assessment_audit_trail` key left on installs that predate DEBUG-305. Its patterns are deliberately anchored so the AES-256-GCM-encrypted safety plan under `crisis_async_` is untouched. This control removes the records; it does not retroactively encrypt or otherwise remediate them into compliance. `clearAllWellnessData` sweeps the same two patterns independently, covering the user who erases their account without relaunching first. | `app/src/core/services/security/legacyPlaintextRecordSweeper.ts`; `app/App.tsx` | launch-time sweep |
| 17 | **No durable local persistence of crisis-path log entries (DEBUG-355).** ERROR-level and CRISIS-category log entries are held in a bounded in-memory ring (1,000 entries) and emitted to the console; they are written to no store. The prior SecureStore write and the dormant log-encryption tier that wrapped it are both deleted, so there is no configuration in which a crisis log entry reaches disk. This is a *stop-processing* control, not a retention control: it is credited because the record no longer exists, not because it is protected. The trail is additionally dropped from memory as the final step of account deletion. | `app/src/core/services/logging/ProductionLogger.ts`; `app/src/core/services/privacy/AccountDeletionService.ts` | `addToAuditTrail` / `clearAuditTrail`; `deleteAccountAndWipe` |
| 18 | **Erasure coverage for storage-access audit logs (DEBUG-355; narrowed by MAINT-378).** `logStorageAccess` persists an entry under `audit_log_<epoch>` on every **failed** storage operation. (It also fired on every `crisis_tier` operation until MAINT-378 deleted that tier as dead code; the failure arm is now the sole writer, so the control's subject matter is strictly smaller than when it was written, and the prefix remains swept.) That prefix is part of `SWEPT_ASYNC_PREFIXES`, the single constant `clearAllWellnessData` filters on and the erasure privacy suites assert against — replacing the hand-copied prefix mirrors that previously let production and test drift apart unnoticed. Because these keys enumerate, the control reaches records already written on shipped installs; no sweeper entry or persisted index is required. | `app/src/core/services/security/SecureStorageService.ts`; `app/src/core/services/security/__tests__/auditLogErasure.privacy.test.ts` | `SWEPT_ASYNC_PREFIXES` / `clearAllWellnessData` |
| 19 | **Erasure coverage for the storage metadata index (DEBUG-381).** `storage_metadata_index` is an unencrypted AsyncStorage key holding one record per stored blob — `storageKey` (e.g. `assessment_async_<assessmentId>`), `storageTier: assessment_tier`, `sensitivityLevel: level_2_assessment_data`, `dataType`, and access timestamps. It contains no wellness content, but establishes that a PHQ-9/GAD-7 record existed, when, and how often it was read (the example was a `crisis_tier` record until MAINT-378 removed that tier; the survivor is unchanged, only which tiers can populate it). It matched no swept prefix and survived account deletion. **The control is the ORDERING, not merely the sweep entry, and crediting only the latter would be an affirmatively false control claim.** `storeMetadata` re-serialises the entire in-memory `metadataCache` on every write and the sweep did not clear that cache, so adding the key to `SWEPT_EXACT_KEYS` alone would delete the file and let the next assessment write restore it verbatim, erased records included. The fix clears the cache **synchronously, before the sweep's first `await`** — any later position reopens a window in which an in-flight write captures the pre-erasure map — and applies unconditionally on both the account-deletion and logout branches. Because the key is a single fixed AsyncStorage name, the control reaches records already written on shipped installs, with no migration and no sweeper entry. Pinned by a regression test that asserts the erased record id does not reappear after a *subsequent* write; the sweep-only non-fix passes every other assertion in that suite and fails exactly that one. | `app/src/core/services/security/SecureStorageService.ts`; `app/src/core/services/security/__tests__/storageMetadataIndexErasure.privacy.test.ts` | `metadataCache.clear()` + `SWEPT_EXACT_KEYS` / `clearAllWellnessData` |

| 20 | **Lapsed consent is a restriction of processing, mechanically enforced, and the ordinary retention schedule continues to run through it (FEAT-399).** When a stored consent record predates `CONSENT_VERSION` or has expired, `loadConsent` resolves `consentStatus` to `version_mismatch` / `expired` and `canPerformOperation` returns false for **all five** operations (analytics, crash reports, cloud sync, research, mental-health processing) — it gates on `consentStatus !== 'valid'`, so this is enforced by the code path rather than by policy. Retained wellness data is not deleted at the moment of lapse; retention is still **processing** under GDPR Art. 4(2), and there is no legitimate-interest fallback for special-category data, so the state is characterised as **restriction of processing** (Art. 18) rather than as continued processing under a lapsed basis. **The time bound is the existing published schedule, not a new one**: `DataRetentionService.runRetentionCleanup()` is invoked unconditionally and independently of consent state at every launch (`app/App.tsx`, inside the startup `Promise.allSettled`), applying `DEFAULT_RETENTION_DAYS = 90` — the `privacy-policy.md` §7.1 commitment — with crisis data at `CRISIS_RETENTION_YEARS = 3` per §7.2. No lapse-specific retention period is asserted here, deliberately: a shorter published bound would be a commitment no shipped code enforces, and the sweep that would enforce one is a tracked follow-up rather than part of this slice. The supported exits from the restricted state are `renewConsent` (re-grant, allowlisted to exactly these two statuses, with 18+ eligibility **re-derived** from `birthYear` rather than read from the stored record's flag) and `declineReConsent` (refusal, which writes no consent record so the restriction persists and the user is re-prompted). Both append to the encrypted Art. 7(1) audit chain via a reload rather than the in-memory array, which is empty in precisely these two states. | `app/src/core/stores/consentStore.ts`; `app/src/core/services/data-retention/DataRetentionService.ts`; `app/App.tsx` | `canPerformOperation` / `renewConsent` / `declineReConsent`; `DATA_RETENTION_CONFIG` |

---

## 8. Residual Risk Acceptance

After applying the controls inventoried in §7, residual **likelihood** for all four scenarios in §6 is assessed as Low. Residual **impact** is Low for scenarios 1, 3, and 4. For scenario 2 (unauthorized device access), residual impact remains High — the layered controls reduce likelihood to Low, but successful exploitation of the device-unlock plus OS-keystore chain would expose sensitive wellness content. The per-scenario rationale is recorded in §6.1.

**Acceptance.** Palouse Labs LLC accepts the residual risk for the processing described in §2 on the basis that:

1. No single mitigation in §7 is the sole defense for any scenario in §6 — controls are layered.
2. Local-first architecture limits attacker yield in cloud scenarios to encrypted blobs.
3. The user retains direct control over device access and over whether to enable cloud backup.
4. No technical control can eliminate device-loss risk; for scenario 2, the user's enrollment of a device passcode/biometric is the irreducible final layer.

**Accepted residual — `critical_log_*` records on pre-fix installs (v1.9, DEBUG-355).** Control 17 stops the write, but it cannot reach records already on a device. Those records are **irreducible**, not merely unremediated: they live in SecureStore under `critical_log_<epoch>` keys, SecureStore exposes no enumerate API, and no index of what was written was ever persisted. A remediation sweeper is therefore not a "not yet built" item — it is not constructible without first shipping a build that records an index of the very keys we want gone, which would mean writing *more* of this data to delete some of it. Palouse Labs accepts this residual on the following basis:

1. **Bounded and shrinking.** No new records are created from this version onward. The existing set is fixed and decays with app reinstalls and device turnover.
2. **Confined and OS-protected.** The records never left the device — no transmission, no sub-processor, no server copy. They sit in the iOS Keychain / Android Keystore, i.e. behind the same OS-protected store credited as control 2, so their exposure profile is scenario 2 (unauthorized device access), already assessed at Low likelihood.
3. **Content is derived and bounded, not raw.** Entries are sanitized log records: message, category, level, timestamp, and an anonymous session hash. `ProductionLogger.crisis()` accepts only `detectionTime` / `interventionType` / `severity` by construction and no scores, responses, Q9 values, or identifiers.
4. **The erasure right is honoured going forward, and the master-key deletion limits reach.** A user exercising deletion today gets every current-generation record removed. Pre-fix residue is not re-created and holds no key material.
5. **No notification obligation arises.** Consistent with the v1.8 assessment of the DEBUG-305 records: local-only, never transmitted, no evidence of unauthorized access, so no 16 CFR Part 318 trigger. This is a self-identified defect closed by us, not an incident.

Recorded here rather than left silent because §3 and `lia-crisis-telemetry.md` §4 safeguard 6 both previously asserted that no durable local crisis record persists. That assertion was false while these writes existed; it becomes true prospectively with control 17, and this residual is the honest qualification on the retrospective half.

**Backstop.** Should any of the residual-risk scenarios materialize as an actual breach, the Federal Trade Commission's Health Breach Notification Rule (16 CFR Part 318) governs the notification obligation. User-facing notification procedures are documented in `docs/legal/privacy-policy.md` §4.4.

---

## 9. Review Schedule and Change Log

**Scheduled review:** at minimum every 12 months from the effective date in §1.

**Off-schedule review triggers:** per the material-change clause in §1.

**Change log:**

| Version | Date | Author | Change |
|---|---|---|---|
| 1.0 | 2026-05-24 | Palouse Labs LLC | Initial assessment surfaced by INFRA-83 |
| 1.1 | 2026-05-30 | Palouse Labs LLC | INFRA-199: PostHog now evaluates feature flags (`cloud_sync`, `cross_device_sync`, `emergency_sync`) in addition to capturing behavioral events. Flag evaluation payload confirmed to contain only anonymous `distinct_id` and `surface: 'app'` super-property — no sensitive wellness data categories from §3 are transmitted as targeting properties. Data scope of PostHog as sub-processor is unchanged. Analytics-consent gate remains the condition for PostHog provider mounting; cloud_sync consent gate remains the independent condition for data transmission to Supabase. Reviewed under INFRA-199 compliance pass. |
| 1.2 | 2026-06-03 | Palouse Labs LLC | INFRA-214 T5: analytics consolidation. (1) Added crisis-detection telemetry as a new processing activity (§2, §4): the `crisis_detected` event (PHQ-9 ≥20 / Q9>0 / GAD-7 ≥15) is recorded to first-party Supabase `analytics_events` under GDPR Art. 6(1)(d)/9(2)(c) vital interests, without analytics consent, with a PII-free bucketed payload (no raw score/Q9). Full lawful-basis record: `lia-crisis-telemetry.md`. (2) Revised §6.1 Scenario 1 to reflect the two-sink model and to state explicitly that k-anonymity / differential privacy are NOT claimed (the dead engine that notionally provided them was deleted in INFRA-214). (3) Added §7 controls 13–14 (PII-free crisis payload; durable lossless capture). (4) Material-change assessment recorded below. |
| 1.3 | 2026-06-08 | Palouse Labs LLC | INFRA-260: identity hardening + control-status correction. (1) **Control 6 correction** — the device-hash RLS credited as a *live* isolation control in v1.0–v1.2 was SQL-editor-verified only and inert on the runtime path (the app never set the `app.device_id` GUC). INFRA-260 replaced it with a Supabase anonymous session so `auth.uid()` is non-null, rewrote all policies to key on it with `WITH CHECK` on writes, and re-verified isolation against the runtime API path (`supabase-rls-verification.md` v2.0). Control 6 is now genuinely live. (2) Control 12 extended: server-side erasure (`delete-account` → `auth.admin.deleteUser` → FK cascade) now backs the right-to-deletion. (3) Subscription receipts encrypted at rest (AES-256-GCM) + IAP transactions bound to one `auth.uid()`. **Material-change assessment:** NOT a §1 trigger — no new data category, no new processor, no local-first change, no new jurisdiction. This is a control-status correction (a credited mitigation made genuinely effective) + a security improvement, not a new processing activity. Founder self-certification. |
| 1.6 | 2026-06-16 | Palouse Labs LLC | INFRA-264: external dead-man's-switch for the crisis-detection alerter. On every clean run the `crisis-detection-alerting` edge function fires a heartbeat to **healthchecks.io** — a NEW third-party sub-processor — so a total Supabase/edge outage (which would blind the in-Supabase watchdog) is caught by the resulting missed ping. The ping is an **opaque HTTP GET**: no request body, no `user_id`/`session_id`, no wellness data, no quasi-identifier — only the fact and timestamp of the request, logged by healthchecks.io for uptime monitoring. **No new sensitive-data category.** Material-change assessment recorded below. (Surfaced + verified live in prod via INFRA-278's deploy-state check, 2026-06-16, which confirmed `CRISIS_HEALTHCHECK_PING_URL` is provisioned.) |
| 1.5 | 2026-06-13 | Palouse Labs LLC | INFRA-265: synthetic crisis-detection liveness probe. A new `pg_cron` job invokes the `crisis-liveness-probe` edge function every 6h, which writes a clearly-tagged **synthetic** marker row to a NEW dedicated `crisis_liveness_probe` table (PII-free synthetic ops telemetry; RLS-on/no-policies/service_role-only/90-day prune, mirroring `crisis_alert_runs`). The INFRA-219 alerter reads `MAX(probed_at)` as an authoritative dead-vs-quiet liveness signal for the ingest/cron/edge leg. **No new sub-processor** (Supabase-internal; no Resend/EAS). **No new sensitive-data category** — the marker holds only a timestamp + a `synthetic_liveness` constant + a status, never user/session id or wellness data. R2 boundary enforced structurally: a separate table the FEAT-129 views cannot reference, plus a belt-and-suspenders CHECK on `analytics_events` that rejects any synthetic-tagged row, so the synthetic signal can never enter the compliance export. Material-change assessment recorded below. |
| 1.7 | 2026-07-25 | Palouse Labs LLC | INFRA-295: Sentry release-health control-status correction (**retrospective**). (1) **Finding** — `ExternalErrorReporter.ts` set `autoSessionTracking: false` at Sentry init, but `@sentry/react-native` reads `enableAutoSessionTracking`; the option was a dead no-op forwarded verbatim to the native layer, and both native SDKs default session tracking ON. Sentry release-health session envelopes have most likely been transmitted from every non-`__DEV__` build since Sentry was activated, including the 2026-06-15 TestFlight build. (2) **Scope of exposure** — session envelopes carry no wellness content (no PHQ-9/GAD-7 responses, scores, journal text, or crisis data) and bypass `beforeSend` / `normalizeDepth` / the FEAT-284 event processor entirely, being envelope session items rather than events. However, because no `Sentry.setUser` call exists anywhere in `app/src`, the iOS native layer injects `user.id = <installationID>` (a persistent per-install UUID) into the scope, and the session envelope's `did` field carries that value to Sentry — a previously-undisclosed identifier reaching an **already-authorized** sub-processor, not disclosure to a new or unauthorized recipient. (3) **Remediation** — the init key was corrected to `enableAutoSessionTracking: true` and session tracking is **kept** as an intentional app-stability signal (crash-free session rate); `docs/legal/privacy-policy.md` §5.1 was updated to disclose session diagnostics and the per-install identifier. A companion correctness fix restored the `mechanism` scalars and `level` to the scrubbed error payload — without them the SDK's `isHardCrash()` never matched, so JS fatal errors were not marking their sessions crashed and any crash-rate signal would have been systematically optimistic. Both are pinned by `app/__tests__/privacy/releaseHealthSession.contract.test.ts`, which imports the SDK's real `isHardCrash` so an upstream change to the crash-attribution rule fails the build. (4) **Not a breach** — no wellness content was exposed; the exposed value is a device-scoped installation identifier, not "PHR identifiable health information" under 16 CFR Part 318, so the FTC Health Breach Notification Rule backstop in §8 is not triggered. **Material-change assessment:** NOT a §1 trigger — no new sensitive wellness data category (§3), no new sub-processor (Sentry already in scope), no local-first architecture change, no new jurisdiction. This is a retrospective control-status correction (documenting and closing a pre-existing configuration defect), not a new processing activity requiring pre-activity assessment. Founder self-certification. **Open operator action:** verify the App Store Connect App Privacy questionnaire declares Diagnostics (Crash Data / Performance Data) as "Not Linked to You" / "Not Used for Tracking". |
| 1.4 | 2026-06-13 | Palouse Labs LLC | INFRA-219: automated crisis-detection alerting. A `pg_cron` job invokes the `crisis-detection-alerting` edge function daily; on a volume-spike or liveness/pipeline-dead breach over the FEAT-129 operator-only views it emails the founder via **Resend** — a NEW third-party sub-processor. The alert payload is aggregate-only and **non-personal**: bucketed counts, category labels, verdict statuses, and a DAY-level date; a ≥3 minimum-count floor withholds rare per-bucket rows from external transmission; it never carries user_id / session_id / distinct_sessions / raw score / Q9 / sub-day timestamp. The function reads ONLY the views, never `analytics_events`. A Supabase-internal watchdog cron escalates if the alerter stops running (shared-failure-domain residual accepted pre-launch; external dead-man's-switch is a tracked follow-up). Material-change assessment recorded below. |
| 1.8 | 2026-08-06 | Palouse Labs LLC | MAINT-334: local crisis-intervention records — control-status correction + coverage. (1) **Control 14 correction** — v1.2 through v1.7 credited a "separate mandatory on-device audit log" as a live control alongside durable crisis-telemetry capture. DEBUG-305 deleted the function that wrote it (`logCrisisIntervention`) and touched no documentation, so from that point the clause described a control that did not exist. It has been removed from control 14. This was an affirmatively false control claim, not merely an omission. (2) **§3 coverage** — the local crisis-intervention path was never enumerated in the data-categories section. It is now, as a coverage note rather than an eighth category: the plaintext `crisis_intervention_*` record is gone at source, the derived signal lives inside the already-enumerated screening categories, and the durable audit trail is the off-device `crisis_detected` event. The note deliberately does **not** claim that no local crisis record persists — `@being/supabase/crisis_analytics_queue` is a real, locally persisted, unencrypted pending-upload buffer, and stating otherwise would have replaced one inaccuracy with another. Its contents (bucketed fields only), its transience, and its erasure are described instead. (3) **§7 controls 15–16 added** — the erasure sweep including the `SWEPT_EXACT_KEYS` exact-name exception list and the reason `crisis_analytics_queue` uses it rather than a swept prefix; and the launch-time `legacyPlaintextRecordSweeper` as the remediation control for records on pre-fix installs, framed as deletion rather than remediation-into-compliance. (4) **Sibling correction** — `lia-crisis-telemetry.md` §4 safeguard 6 carried the same now-false on-device-audit claim, naming `logCrisisIntervention` by function, and that safeguard is load-bearing in the LIA's §3 balancing test for consent-free vital-interests processing. Corrected in the same change so the two artifacts do not disagree. **Material-change assessment:** NOT a §1 trigger — no new sensitive wellness data category (§3 gains a coverage note on existing categories, and the underlying change is a reduction in local footprint, not an expansion); no new sub-processor; no local-first architecture change; no new jurisdiction. This is a control-status correction of an existing data category, in the same class as v1.3 and v1.7. Founder self-certification. **No user-facing notice required** — recording, not re-litigating, the DEBUG-305 compliance ruling: the affected records were local-only and never transmitted, there is no evidence of unauthorized access, so no 16 CFR Part 318 obligation arises; and the post-fix position makes `privacy-policy.md` §4.1 and §7.4 more accurate rather than newly qualified, so no public-notice edit is required. |
| 1.9 | 2026-08-08 | Palouse Labs LLC | DEBUG-355: two further local crisis-path writes — coverage + controls + accepted residual. (1) **§3 correction** — the v1.8 coverage note was written believing the local crisis footprint was closed by DEBUG-305. It was not. Two writes remained, neither enumerated: `critical_log_<epoch>` to SecureStore for every ERROR-level or CRISIS-category log entry, and `audit_log_<epoch>` to AsyncStorage for every `crisis_tier` storage operation and every failure. Both are now described in §3. Note the failure mode is the opposite of v1.8's: v1.8 corrected an affirmatively **false control claim**; this corrects an **omission** that let the surrounding prose read as more complete than it was. (2) **The two are different in kind, and the remedy follows the kind.** `critical_log_*` was unerasable *by construction* — SecureStore has no enumerate API and the key carried a timestamp, so no sweep could ever have found it, and no launch-time sweeper is constructible without first shipping a build that persists an index of the very records we want deleted. The write is therefore deleted at source. `audit_log_*` was unerasable *by omission* — the keys enumerate; the prefix was simply missing from the sweep, and adding it reaches already-shipped installs with no migration. (3) **Encryption tier removed, not repaired.** `critical_log_*` entries were stored in cleartext in every real configuration: the tier's enable path had zero callers, so `encryptionEnabled` was always false and the "encrypt, else store plaintext" branch always took the plaintext arm. What the code presented as a failure fallback was the only path. The tier is deleted rather than wired up, because the compliance ruling is to stop persisting, not to persist more safely. (4) **§7 controls 17–18 added**; §8 gains an accepted-residual entry for records on pre-fix installs, with the irreducibility argument stated rather than deferred to a sweeper that cannot exist. (5) **Sibling correction** — `lia-crisis-telemetry.md` §4 safeguard 6 asserted local exposure is "now nil", which was false while these writes existed and is load-bearing in that assessment's §3 balancing test. Corrected in the same change, as v1.8 did for the same safeguard. (6) **Test-side** — the erasure privacy suites hand-copied the sweep's prefix list, so production and test could drift silently; that mirror is replaced by an import of `SWEPT_ASYNC_PREFIXES`, matching the pattern `SWEPT_EXACT_KEYS` already used. The suite that covered the removed tier asserted the plaintext fallback as required behaviour and is deleted, not rewritten. **Material-change assessment:** NOT a §1 trigger — no new sensitive wellness data category (§3 gains coverage of pre-existing writes, and the change is a reduction in local footprint); no new sub-processor; no local-first architecture change; no new jurisdiction. Control-status correction plus coverage, same class as v1.3, v1.7 and v1.8. Founder self-certification. **No user-facing notice required** — same basis as v1.8: local-only records, never transmitted, no evidence of unauthorized access, so no 16 CFR Part 318 obligation. The post-fix position makes `privacy-policy.md` §4.1 and §7.4 more accurate, not newly qualified. |
| 2.0 | 2026-08-08 | Palouse Labs LLC | MAINT-378: the `crisis_tier` storage tier is **deleted as dead code**, which narrows two control claims this assessment makes. (1) **§3 correction** — the `audit_log_<epoch>` entry added at v1.9 was described as written "on every `crisis_tier` operation and every failure." Only the failure clause survives. The crisis-operation arm is gone with the tier, so the record no longer attests that a crisis-tier record existed and when; that sentence in §3 is corrected rather than deleted, because it remains true of records already on shipped installs. (2) **§7 control 18 narrowed** — same correction, same reason; the prefix stays in `SWEPT_ASYNC_PREFIXES` because the failure arm still writes, so the control is intact, merely smaller in subject matter. (3) **Nothing about erasure coverage is weakened.** `crisis_async_` remains in `SWEPT_ASYNC_PREFIXES` and both `crisis_async_` and `crisis_secure_` remain enumerated by `deleteSecureData`, deliberately and with comments to that effect, as a defensive floor for records on already-shipped installs. Removing a namespace from a deletion path is a one-way regression and was explicitly declined. (4) **Basis for treating this as a reduction, not a change in processing** — the tier had one writer (`SecureStorageService.storeCrisisData`) whose only caller sat in `SecurityMonitoringService`, a service with zero production importers, and no reader at all. Its last genuine caller was removed in MAINT-123 (2025-12-26), roughly six months before the app's first external distribution (TestFlight, 2026-06-15), so **no build that ever reached a real device contained a live write path into this tier**, under either the legacy SecureStore `crisis_secure_` prefix or the current AsyncStorage `crisis_async_` prefix. (5) **Sibling correction** — `legacyPlaintextRecordSweeper` justified its deliberately narrow patterns by asserting that `crisis_async_*` holds the encrypted safety plan and emergency contacts. That was false from the start; the safety plan is not persisted through this service. The narrowness argument is re-grounded on the structural risk of an unsupervised launch-time prefix sweep, which stands on its own. The same false claim is corrected in that module's privacy suite, including a test title. (6) **Pre-existing gap restated, not closed** — legacy SecureStore `crisis_secure_*` keys are still not swept by prefix (no enumerate API, not in `WELLNESS_SECURE_STORE_KEYS`); this change does not alter that, and it must not be represented as having closed it. It is immaterial only because no such record was ever written on a shipped build, per (4). **Material-change assessment:** NOT a §1 trigger — no new sensitive wellness data category (this is a strict reduction in local footprint); no new sub-processor; no local-first architecture change; no new jurisdiction. Control-status correction, same class as v1.3, v1.7, v1.8 and v1.9. Founder self-certification. **No user-facing notice required** — no user data is deleted, moved, or newly exposed; the change removes code that could write a record no shipped build ever wrote. |
| 2.1 | 2026-08-08 | Palouse Labs LLC | DEBUG-381: `storage_metadata_index` — a fourth local wellness-path survivor — added to the erasure sweep, with the in-memory `metadataCache` cleared ahead of it to close a write-back loop that would otherwise have made the sweep entry inert. §3 restructured: the recurring per-defect completeness note is **replaced by a positive enumeration of every persisted key on the wellness/crisis path**, because the negative form ("nothing else remains") had been asserted and falsified in three consecutive versions — v1.8, v1.9, and this one — each time discovered by enumerating the store after erasure rather than by review. §7 gains control 19. Version bumped to 2.0 for the structural §3 change, not for a change in processing. Sequenced after MAINT-378 (also 2026-08-08): that change deleted the `crisis_tier` storage tier, so the survivor this control covers is now populated by `assessment_tier` records. The §3 enumeration and control 19 are written against the post-MAINT-378 tier set, and the widened privacy assertion targets the surviving KEY rather than a tier string, which would otherwise have passed vacuously. |
| 2.2 | 2026-08-10 | Palouse Labs LLC | FEAT-399: the lapsed-consent state is characterised, and the re-consent write path is added. (1) **§7 control 20 added** — a consent record that predates `CONSENT_VERSION` or has expired puts the account into **restriction of processing** (GDPR Art. 18), not into continued processing under a lapsed basis. The restriction was already enforced mechanically before this change (`canPerformOperation` gates on `consentStatus !== 'valid'`); what was missing was the documented basis and a stated time bound. (2) **The time bound is the existing published schedule, adopted by reference — no new number is asserted.** `DataRetentionService.runRetentionCleanup()` already runs unconditionally and independently of consent state at every launch, at `DEFAULT_RETENTION_DAYS = 90` (`privacy-policy.md` §7.1) with crisis data at 3 years (§7.2). **A 30-day lapse-specific bound was proposed during planning and is recorded here as declined**, because its stated justification did not survive inspection: the 30 days in `privacy-policy.md` §7.3/§7.4 is the service-level commitment for honouring a **deletion request**, a different obligation, and the auto-delete sweep that would enforce a 30-day lapse deletion is explicitly out of this slice. Publishing a bound that no shipped code enforces would be an unbacked commitment of exactly the kind FTC Act §5 reaches. The 90-day figure is not a preference; it is the only period currently backed by running code. (3) **A minor-protection defect was found and closed rather than inherited.** DEBUG-150 (`c96ab71e`) flipped the age gate 13+ → 18+ **in the same commit** that bumped `CONSENT_VERSION` 1.0.0 → 1.1.0, and 1.1.0 remains the only changelog entry. Every v1.0.0 record — the only cohort a `version_mismatch` re-consent can ever serve — therefore carries an `ageVerification.isEligible` flag computed under the retired 13+ rule, so `true` on such a record means "≥13", not "≥18". The flag's type is unchanged across that boundary while its meaning is not, which is invisible to the compiler and to every pre-existing test. `renewConsent` accordingly **re-derives** eligibility from `birthYear` and refuses when `birthYear` is absent, rather than trusting the stored flag; `MINIMUM_CONSENT_AGE` is now a single named constant so the gate has one definition. This is prospective hardening, not an incident: the 18+ flip predates the first external distribution (TestFlight, 2026-06-15), so no shipped build is known to have written a 13-to-17 record. (4) **`renewConsent` does not write `legal_gate_consents_v1`.** That record's four booleans attest that the user was shown and accepted named documents; this slice has no UI, so stamping a fresh version into it would manufacture Art. 7(1) evidence of an acceptance that never occurred — on a plaintext key that is erasure-excluded and would outlive account deletion. Consequence recorded rather than left silent: after a renewal that record keeps its old version, so two version fields diverge on a demonstrability artifact. Nothing reads it for staleness today, so there is no live defect; closing it is an inherited obligation of the re-consent screen slice, noted at the write site. (5) **Renewed preferences are supplied by the caller, never carried forward.** A v1.0.0 record predates `mentalHealthProcessingConsent` — the exact key named in `CONSENT_CHANGELOG['1.1.0'].changedKeys` — so copying preferences forward would fabricate or silently default the Art. 9(2)(a) explicit consent that the 1.1.0 bump exists to collect. The same reasoning binds the `expired` branch: refreshing an expiry without a user act is not consent. (6) **`universalOptOut` carries forward verbatim.** It is the GPC-equivalent universal opt-out signal; CCPA/CPRA (Civ. Code §1798.135(b)(1)), TDPSA §541.055(f) and CPA §6-1-1306(1)(a)(IV)(B) each bar requiring a consumer to re-assert an opt-out, and an opt-out is not version-scoped. (7) **Audit-chain integrity.** `loadConsent` returns from both the `version_mismatch` and `expired` branches before it loads history, so the in-memory array is empty in exactly the two states these actions run in; both actions reload the encrypted history before appending. Without that, a renewal would have overwritten the entire Art. 7(1) chain with a single entry. (8) **Vocabulary** — `ConsentHistoryEntry.action` gains `'declined'`. `'revoked'` was rejected as legally wrong (a refusal to re-grant is not an Art. 7(3) withdrawal, and conflating them would collide with the FEAT-316 ordering that keeps a withdrawn user from being re-prompted); `'updated'` was rejected because it asserts a preference change when a decline necessarily makes none, and this vocabulary is exported verbatim into the CCPA/DSR payload, making an inaccurate member an Art. 5(1)(d) defect in the artifact that exists for Art. 7(1) demonstrability. `fromVersion` / `toVersion` are typed **optional** fields rather than `note` prose, so every previously persisted entry deserialises unchanged and no regulator or data subject has to parse free text to recover which policy version was consented to. **Material-change assessment:** NOT a §1 trigger — no new sensitive wellness data category, no new sub-processor, no local-first architecture change, no new jurisdiction. This is a characterisation of an existing state plus a control-coverage addition, in the same class as v1.3, v1.7, v1.8, v1.9, v2.0 and v2.1. Founder self-certification. **No user-facing notice required, and `privacy-policy.md` is deliberately unedited** — the restriction-of-processing characterisation adds no new public commitment and changes no published retention period, so a policy edit would carry no compliance benefit while touching the legal-site sync path. `CONSENT_VERSION` is unchanged at 1.1.0; these actions are inert until the re-consent screen slice lands. |
| 2.3 | 2026-08-12 | Palouse Labs LLC | INFRA-379: pre-activity record for four merged-but-unapplied migrations being applied to production together, and the arming of the ops dead-man's-switch. **This entry exists because the pattern nearly broke.** v1.4, v1.5 and v1.6 each logged a new production `pg_cron` job *before* go-live, expressly to produce the pre-activity record TDPSA §541.105(a) / CPA §6-1-1309 / VCDPA §59.1-580 / CTDPA §6 contemplate. INFRA-266, INFRA-282, INFRA-296, DEBUG-340 and MAINT-347 all shipped code without one — which went unnoticed precisely because **merging is not deploying** for Supabase (no CI hook), so none of them ever became live processing and the omission had no effect until now. Applying them is the moment that changes. (1) **New scheduled jobs:** `grace-period-automation` (daily 02:00 UTC, INFRA-266) and its 90-day heartbeat prune; `subscription-verification-watchdog` (6-hourly, INFRA-282); `analytics-retention-prune` (daily 04:20 UTC, DEBUG-340, re-pointed by MAINT-347 at a heartbeat wrapper) and `retention-prune-runs-prune`. (2) **New tables/views:** `grace_period_automation_runs`, `retention_prune_runs`, `retention_prune_health` — all PII-free ops counters and timings, RLS-on with no policies, `service_role` only, each self-pruning at 90 days. No `user_id`, no subscription or receipt data, no wellness data of any kind. (3) **A deletion capability goes from dormant to live.** `cleanup_old_analytics()` has existed since the base schema and was never scheduled, so real server-side retention of `analytics_events` has been **indefinite** — over-retention against the 90-day/3-year periods §7.1/§7.2 already publish. DEBUG-340 makes the function category-aware (90 days general, 3 years `crisis_detected`) *before* scheduling it, so activation closes the gap rather than converting a silent over-retention into an active under-retention. **Pre-flight census taken immediately before the push: `analytics_events` held exactly one row, `event_type='crisis_detected'`, created 2026-07-26** — inside the 3-year tier, so the first scheduled run deletes nothing. No new period is asserted and no user-facing notice arises; practice moves to match an already-published promise. (4) **`cleanup_orphaned_backups()` is DROPPED, not scheduled** — the opposite call to (3), and deliberately so. §7.3 promises settings-backup retention "until you disable backup or request deletion", both **user acts**; that function deleted on 180 days of inactivity with no user act, so scheduling it would have *created* a violation. Its unscheduled state was what kept the promise. Censused at 0 rows, so the drop destroys nothing. (5) **No new sub-processor.** Resend (v1.4) and healthchecks.io (v1.6) are both already in the inventory. The ops domain uses distinct Vault names (`subscription_alert_*`) and its own healthchecks.io check so the two trust domains rotate and page independently; the underlying Resend account and founder inbox may be shared, which is a rotation-discipline matter, not a disclosure one. The ops ping is the same opaque bodyless GET assessed at v1.6. (6) **A trust-domain defect was found and closed in the same change, not inherited.** All three cron-invoked edge functions read the project-wide `CRON_SECRET` edge secret, while `20260616000000` simultaneously instructed the operator that the grace-period bearer must be distinct from the crisis one — unsatisfiable, since Supabase has no per-function secret scoping. Left alone it resolves one of two ways: every grace-period tick 401s (heartbeat never lands, watchdog pages correctly about a pipeline that was never reachable), or the operator "fixes" it by pointing both domains at one bearer, where an ops-side rotation silently breaks **crisis** paging. `grace-period-automation` now reads its own `GRACE_PERIOD_CRON_SECRET`, pinned by `supabase/functions/_tests/cron-secret-trust-domains.test.ts`. **Material-change assessment:** the *new-processing-activity* limb is met (new scheduled jobs + new tables), so this amendment is the required pre-activity record, on the same footing as v1.5. No other trigger is met — **no** new category of sensitive wellness data (every new column is a PII-free count, timestamp or status; the retention change narrows retention rather than widening collection), **no** new sub-processor, **no** local-first architecture change, **no** new jurisdiction. **Founder self-certification** suffices pre-launch per §10. **No user-facing notice required and `privacy-policy.md` is deliberately unedited** — §7.1/§7.2/§7.3 already state every period involved, and after (4) the code matches all three; publishing nothing new means nothing new to notice. |
| 2.4 | 2026-08-14 | Palouse Labs LLC | INFRA-379 (second entry): two grant defects found by *deploying* the switch v2.3 recorded. Both surfaced within ninety seconds of the first-ever test-fire of `grace-period-automation`, and neither is a regression — both are original omissions that had never been exercised, because there are no live subscribers and no receipt has ever been verified in production. (1) **`service_role` held no DML on `public.subscriptions`.** `20260523000000_base_schema.sql` §16 granted `SELECT, INSERT, UPDATE` to `authenticated` and never to `service_role`; `20260607120000`'s REVOKE targeted `anon` only, so INFRA-260 did not cause it. Effect: every service_role edge function doing a direct `.from('subscriptions')` failed `permission denied` — `verify-apple-receipt`, `verify-google-receipt`, `subscription-webhook` and `grace-period-automation`, i.e. the whole server-side subscription lifecycle. `delete-account` unaffected (uses `auth.admin.deleteUser()`, no direct table access); the SECURITY DEFINER RPCs unaffected, which is why it presented as one partial error rather than a dead function. Fixed by `GRANT SELECT, INSERT, UPDATE` — the same verb set §16 chose for `authenticated` on the same table. **DELETE deliberately withheld** (no call site deletes; a standing delete privilege on a table holding encrypted receipt data is exactly the unbacked grant minimisation argues against), and the fix is **scoped to `subscriptions` only** — `users` and `encrypted_backups` carry the identical gap but have zero direct call sites, and pre-granting them would be the same over-grant, on a table the DPIA credits precisely because Supabase has no operational reason to touch its rows. Follow-up ticket to grant specific verbs when a real call site exists. (2) **Three cron-only SECURITY DEFINER functions were executable by `anon`.** `20260618000000`'s `REVOKE ALL … FROM anon, authenticated` was inert: Postgres grants EXECUTE to PUBLIC by default and revoking from a role does not touch the PUBLIC grant it inherits (live catalog confirmed `proacl` carrying the leading `=X`). Since PostgREST exposes `public` functions at `/rest/v1/rpc/<name>` and the anon key ships inside the mobile app, an unauthenticated caller could trigger `subscription_verification_watchdog()` (founder alert emails on demand — an unbounded outbound-send and Resend-quota vector) and `run_analytics_retention_prune()` / `cleanup_old_analytics()` (forcing the retention delete early). The deletion exposure is bounded — those functions only remove rows already past the §7.1/§7.2 windows, so the worst case is deletion before 04:20 UTC rather than at it, and **no row was deleted before its published period**; the email path is the material one. All three now `REVOKE EXECUTE … FROM PUBLIC` plus explicit role revokes. Safe by construction: pg_cron runs each job as its owner (`postgres`), which owns the functions. **`crisis_alert_watchdog()` (INFRA-219) has the identical exposure and is deliberately NOT fixed here** — it is pre-existing rather than introduced by this deploy, and it triggers sends in the *crisis* trust domain, so it belongs behind a `crisis` agent pass per the Protected Paths table rather than appended to an ops migration. Recorded here so the known-and-deferred status is on the record rather than resting in someone's memory. **Material-change assessment:** NOT a §1 trigger — no new category of sensitive wellness data, no new sub-processor, no local-first architecture change, no new jurisdiction. Both halves are control-status corrections restoring documented-but-never-live behaviour, the same class as v1.3, v2.0 and v2.1; (2) is a strict tightening. Founder self-certification per §10. **No user-facing notice required** — no new commitment, no published period altered, and no data was retained or deleted outside what §7.1/§7.2 already state. |

**INFRA-214 T5 material-change assessment (2026-06-03).** Assessed against the §1 triggers: *new derived category of sensitive wellness data* — **yes** (`crisis_detected` encodes a trigger/severity category derived from PHQ-9/GAD-7; a new processing activity within the §3 categories); *new third-party processor* — **no** (Supabase `analytics_events` is first-party, already in scope per §2/§5); *local-first architecture change* — **no** (raw PHQ-9/GAD-7 responses remain local-only; this is a server-side write of a derived category); *new jurisdiction* — **no**. **Conclusion:** the revise-trigger is met; this v1.2 amendment is the required pre-activity assessment under TDPSA §541.105(a), CPA §6-1-1309, VCDPA §59.1-580, and CTDPA §6. **Founder self-certification** suffices pre-launch (no EU/EEA base near the §10 500-user threshold); counsel review of the Art. 6(1)(d)/9(2)(c) basis is required before that threshold per §10.

**INFRA-219 material-change assessment (2026-06-13).** Assessed against the §1 triggers: *new category of sensitive wellness data* — **no** (no new category; this is downstream operational use of the already-recorded `crisis_detected` aggregate from v1.2). *New third-party processor receives sensitive wellness data* — **processor yes, sensitive personal data no**: Resend is a new transactional-email sub-processor, but the alert payload it carries is aggregate, non-personal data that cannot be linked to an identified or identifiable natural person (no `session_id` / `user_id`; a ≥3 minimum-count floor on per-bucket rows; day-level date granularity only), so it falls outside the "sensitive wellness data" boundary of the §1 trigger and the "sensitive data" definitions in TDPSA §541.001, CPA §6-1-1303, VCDPA §59.1-575, and CTDPA §1; the recipient is the data controller's own founder mailbox. *Local-first architecture change* — **no**. *New jurisdiction* — **no**. **Conclusion:** the revise-trigger is met on the new-sub-processor limb; this v1.4 amendment is the required pre-activity record. **Resend is added to the sub-processor inventory**, and a signed Resend DPA (standard click-through) must be on file before the alerter goes live. k-anonymity / differential privacy are NOT claimed for the payload (consistent with the runbook and v1.2). **Founder self-certification** suffices pre-launch per §10.

**INFRA-265 material-change assessment (2026-06-13).** Assessed against the §1 triggers: *new processing activity* — **yes** (a new scheduled `pg_cron` job + a new `crisis-liveness-probe` edge function + a new `crisis_liveness_probe` table; this v1.5 amendment is therefore the required pre-activity record under TDPSA §541.105(a), CPA §6-1-1309, VCDPA §59.1-580, and CTDPA §6). *New category of sensitive wellness data* — **no**: the probe is purely synthetic ops telemetry and the marker table holds only a timestamp, the `synthetic_liveness` constant, a status, and an optional duration — **no** user_id / session_id / score / Q9 / wellness data, so it is not "sensitive data" under TDPSA §541.001, CPA §6-1-1303, VCDPA §59.1-575, or CTDPA §1. *New third-party processor* — **no** (Supabase-internal: pg_cron + edge function + a Supabase table; no Resend, no Expo/EAS — the rejected Option C would have added EAS and would have required a further sub-processor entry before going live). *Local-first architecture change* — **no**. *New jurisdiction* — **no**. **Structural integrity controls (R2):** the synthetic marker can never be miscounted as a real crisis because (a) it lives in a dedicated table the FEAT-129 `crisis_detection_*` views (`WHERE event_type = 'crisis_detected'`) physically cannot reference, and (b) a belt-and-suspenders CHECK on `analytics_events` rejects any row carrying a `probe_type` tag, so accidental routing fails loud at the DB layer rather than silently inflating the monthly compliance export. **Conclusion:** the revise-trigger is met on the new-processing-activity limb; the probe itself processes no sensitive personal data. **Founder self-certification** suffices pre-launch per §10.

**DEBUG-381 material-change assessment (2026-08-08).** Assessed against the §1 triggers: **not a material change, no trigger met.** No new category of sensitive wellness data is collected, derived, or inferred — `storage_metadata_index` was already being written and is now being *erased*, which narrows retention rather than widening it. No new third-party processor is involved; the key never leaves the device. The local-first architecture is unchanged and nothing becomes server-decryptable. No new jurisdiction. This is a control-status correction plus coverage, the same class as v1.3, v1.7, v1.8 and v1.9, and founder self-certification is sufficient. No 16 CFR Part 318 (FTC HBNR) obligation arises: the data was local-only, never transmitted, never accessed by an unauthorized party, and the defect was self-identified. Unlike the `critical_log_*` residual recorded in §8, this one needs no residual entry — the key is a single fixed AsyncStorage name, so the fix reaches already-shipped installs with no migration and no launch sweeper. **One residual is recorded rather than closed:** the index remains unencrypted for the life of an install. DEBUG-381 makes it erasable, not confidential; minimising or encrypting its contents is a separate, untaken decision.

**INFRA-264 material-change assessment (2026-06-16).** Assessed against the §1 triggers: *new category of sensitive wellness data* — **no** (the external switch carries no data at all). *New third-party processor receives sensitive wellness data* — **processor yes, sensitive personal data no**: healthchecks.io is a new uptime-monitoring sub-processor, but it receives a bare HTTP GET ping — no request body, no user identifier, no session identifier, no wellness data, and no metadata that could be linked to an identified or identifiable natural person. The sole signal is the fact of the request and its timestamp, internal operational telemetry logged by healthchecks.io for uptime monitoring. This falls outside the "personal data" definition under GDPR Art. 4(1) and outside the "sensitive data" definitions in TDPSA §541.001, CPA §6-1-1303, VCDPA §59.1-575, and CTDPA §1. (The word "health" in healthchecks.io denotes service uptime, not wellness data — no health information of any kind is transmitted.) *Local-first architecture change* — **no**. *New jurisdiction* — **no**. **Conclusion:** the revise-trigger is met on the new-sub-processor limb; this v1.6 amendment is the required pre-activity record. **healthchecks.io is added to the sub-processor inventory**; its standard DPA/ToS should be on file, and the ping URL (`hc-ping.com/<uuid>`) is a capability secret handled per the crisis-analytics runbook (never committed — enforced by the `crisisAlertNoSecrets.config.test.ts` static pin). **Founder self-certification** suffices pre-launch per §10.

---

## 10. GDPR Article 35 Scope Note

A full GDPR Article 35 DPIA has been intentionally scoped out of this document. Being currently has minimal EU/EEA user presence. GDPR Art. 35 has no numeric threshold for when a DPIA is required — the test is qualitative ("high risk to the rights and freedoms of natural persons," with the WP29/EDPB nine-criterion guidance). Being has adopted the following self-imposed conservative triggers, which are stricter than the GDPR qualitative test alone: promote this DPIA into a GDPR-conformant artifact (a) before EU/EEA user count exceeds 500, or (b) before any feature constituting "large-scale processing of special category data" under Art. 9 is enabled — whichever occurs first. The above is itself a material-change trigger per §1.

---

## 11. Statutory Citations Appendix

The following statutes require a documented data protection assessment for processing of sensitive personal information. Mental and physical health condition data is enumerated as sensitive in each statute's definitional section.

### Texas Data Privacy and Security Act (TDPSA)

- **Citation:** Tex. Bus. & Com. Code §541.105(a).
- **Effective:** July 1, 2024.
- **Sensitive data definition (Tex. Bus. & Com. Code §541.001):** Personal data revealing physical or mental health diagnosis is enumerated as sensitive.
- **Assessment requirement (§541.105(a)):** A controller "shall conduct and document a data protection assessment" for processing of sensitive data.

### Colorado Privacy Act (CPA)

- **Citation:** C.R.S. §6-1-1309.
- **Effective:** July 1, 2023.
- **Sensitive data definition (C.R.S. §6-1-1303):** Personal data revealing mental or physical health condition is enumerated as sensitive.
- **Assessment requirement (§6-1-1309):** Controllers must conduct a data protection assessment of processing activities that involve sensitive data.

### Virginia Consumer Data Protection Act (VCDPA)

- **Citation:** Va. Code §59.1-580.
- **Effective:** January 1, 2023.
- **Sensitive data definition (Va. Code §59.1-575):** Personal data revealing mental or physical health diagnosis is enumerated as sensitive.
- **Assessment requirement (§59.1-580):** Controllers shall conduct and document a data protection assessment for processing of sensitive data.

### Connecticut Data Privacy Act (CTDPA)

- **Citation:** Conn. Pub. Act 22-15 §6.
- **Effective:** July 1, 2023.
- **Sensitive data definition (§1):** Personal data revealing mental or physical health condition or diagnosis is enumerated as sensitive.
- **Assessment requirement (§6):** Controllers must conduct and document a data protection assessment of processing of sensitive data. The Connecticut requirement is substantively parallel to the Colorado requirement.

### Federal Trade Commission Act, Section 5

Not an assessment-requiring statute. Listed for context: misrepresentation in this DPIA, in Being's privacy policy, or in any user-facing surface would constitute a "deceptive practice" under 15 U.S.C. §45.

### Federal Trade Commission Health Breach Notification Rule

- **Citation:** 16 CFR Part 318.
- Applies to non-HIPAA entities (including Being) that handle "PHR identifiable health information." Sets a 60-day notification window. Operationalized in `docs/legal/privacy-policy.md` §4.4. Referenced here as the breach-time backstop to the residual-risk acceptance in §8.

---

## Related Documents

- `docs/legal/privacy-policy.md` — user-facing privacy notice. §4 (Data Storage & Security) is the public counterpart to this DPIA's §7.
- `docs/legal/regulatory-applicability.md` — authoritative source of what regulations apply to Being and what framing language to use.
- `docs/legal/california-privacy.md` — CCPA/CPRA-specific consumer rights and disclosures.
- `docs/security/security-architecture.md` — cited throughout §7 of this DPIA.
- `docs/security/supabase-rls-verification.md` — cited throughout §6 and §7 of this DPIA.
- `docs/development/audits/INFRA-83-rescope-2026-05-24.md` — surfacing audit.

| 2.3 | 2026-08-10 | Palouse Labs LLC | MAINT-401: stack-frame side-channel ruling + removal of a dead filter. (1) **Control 10a added** — records the decision that a wellness-sensitive segment in `frames[].filename` / `frames[].function` is **coarsened** against the shared `isSensitiveRoute` keyword set rather than transmitted raw or used to drop the event. The bare-exemption alternative is recorded as **considered and declined**: it would recreate via a stack-frame slot the same sensitive-surface signal `sanitizeScreenName` closes. The exemption is scoped structurally to the frame slots, never as a path-shape heuristic, because such a heuristic would exempt path-shaped *content* (`crisis_alerts/insert score=21`) wholesale. `applyAllowlist`'s four-scalar frame rebuild is named as the compensating control and is now test-pinned. (2) **Control 10b added** — states plainly that 10a is **prospective hardening with no observable production effect today** (rewrite-frames discards real paths before `beforeSend` on every shipping build; symbolication is `__DEV__`-only where the DSN is empty; release bundles are minified; no source-map upload is configured), names the three falsifiers that would make it live, pins the analysis to `@sentry/react-native` 7.11.0, and separately notes — without fixing — that the base64 scrub heuristic blanket-redacts long file paths by character count. (3) **Dead filter removed** — `isCrisisRelated`, a pre-filter on the zero-caller `reportError` path, is deleted rather than aligned. It advertised a control it did not provide: strictly weaker than the `beforeSend` chokepoint (it guarded one entry point) and strictly broader in keywords ('assessment', 'score', 'safety', bare 'phq'/'gad'), such that wiring the path up would have silently suppressed every report from the assessment and crisis surfaces. Aligning it would have preserved a dead duplicate of a live guarantee in citable, control-looking form — the same defect class the v1.8 and v2.0 corrections were written to fix. The wholesale-drop guarantee is untouched. (4) Stale line citations in §6.1 Scenario 4 and control 10 corrected. **Material-change assessment:** NOT a §1 trigger — no new data category, no new sub-processor (Sentry already in scope), no local-first change, no new jurisdiction. Control-status clarification plus a prospective strengthening. Founder self-certification. No 16 CFR Part 318 trigger: nothing was exposed. |

---

*End of document. Internal compliance artifact — not for public distribution.*
