# `docs/legal/` — the source of truth for published legal text

Every user-facing legal document Being publishes originates here. Nothing
downstream is hand-maintained, and nothing downstream should be edited directly.

Two documents in this directory are **not** published anywhere, and two more are
published to one surface only — see [What is published where](#what-is-published-where).

---

## Who consumes this directory

Three surfaces, three different mechanisms. None of them reads these files at
runtime — every one bakes the text in at build time, which is why a stale build
is the failure mode that matters.

### 1. The mobile app (self-healing)

`app/scripts/generate-legal-content.js` reads these `.md` files and emits
`app/src/features/profile/content/legalContent.generated.ts`, which
`legalDocuments.ts` imports.

That generated file is **gitignored** (`app/.gitignore:71`) and regenerated by
`postinstall`, `prestart`, `preios`, and `preandroid`. There is no committed
artifact that can drift from these sources. The generator **throws** on a
missing source rather than emitting stale text, and the consumer imports it, so
an absent file also fails typecheck and Metro bundling.

`scripts/legal-registry.js` (run by CI's *Security + compliance* job) asserts
that the filenames here, the generator's `SOURCES` list, and the app's
`LegalDocumentType` union + `legalDocuments` record all agree.

**Practical consequence:** the app cannot ship text older than its source — but
it also cannot ship text *newer* than its binary. A correction here reaches
users only when a new build ships and clears App Review.

### 2. being.fyi (NOT self-healing — this is the fragile one)

The website does not contain these files. Its deploy workflow
(`being-website/.github/workflows/deploy.yml`) sparse-checkouts this repo at
build time, copies `docs/legal/` into `content/legal/`, and the Next.js pages
`import` the markdown as a raw module. **The text is baked into the deployed
bundle: the only way stale text leaves the site is a rebuild.**

That workflow triggers on pushes to *being-website's own* `main`/`preview`, and
on `workflow_dispatch`. **Nothing in this repo triggers it.** So editing a file
here changes the app's source and leaves the live website exactly as it was.

On 2026-08-07 the live privacy policy was found **68 days and four versions
stale**, publicly serving a crisis-data claim DEBUG-333 had already removed as
false. Nothing detected it for ten weeks.

To publish a change here to being.fyi, **you must trigger the deploy yourself**:

```bash
gh workflow run deploy.yml -R mp2ez/being-website
```

Automating that trigger is tracked in **INFRA-363**, along with the open
question of which ref the website should deploy from (see
[The ref question](#the-ref-question-open)).

### 3. Local website preview (a symlink — and a trap)

`being-website/content/legal` is a **symlink** to
`../../being/development/docs/legal`, and it is itself gitignored
(`being-website/.gitignore:59` — *"Legal content (symlinked locally, copied in
CI)"*).

It exists for local dev only. **Production never touches it.** Two traps follow:

- **Local preview shows the `development` worktree, not what is deployed.** If
  you `npm run dev` in being-website and see current text, that proves nothing
  about the live site. Verify production with `curl`, never by reading a
  worktree.
- **The relative path is load-bearing.** Clone being-website somewhere that
  `../../being/development/docs/legal` does not resolve and local preview
  silently renders nothing.

> **Method note, learned the hard way (INFRA-348).** A local worktree is the one
> place where `main`, `development`, and the deployed state collapse into a
> single filesystem view. Two successive investigations of this exact area drew
> opposite — and both wrong — conclusions by reasoning from `cat`. The checks
> that actually separate the states are `git show origin/<ref>:docs/legal/<file>`
> and `curl` against production.

---

## What is published where

| File | Mobile app | being.fyi route |
|---|---|---|
| `privacy-policy.md` | ✅ | `/privacy` |
| `terms-of-service.md` | ✅ | `/terms` |
| `support.md` | ✅ | `/support` |
| `multi-state-privacy.md` | ✅ | `/privacy/multi-state` |
| `california-privacy.md` | ✅ | `/privacy/california` |
| `medical-disclaimer.md` | ✅ | — (no website route) |
| `regulatory-applicability.md` | — | — internal, compliance source of truth |
| `dpia-sensitive-wellness-data.md` | — | — internal / regulator-facing (INFRA-153) |
| `breach-notification-runbook.md` | — | — internal, founder + counsel (INFRA-152) |
| `lia-crisis-telemetry.md` | — | — internal / regulator-facing (INFRA-214) |

App exclusions are enforced by `EXCLUDED_FROM_REGISTRY` in
`scripts/legal-registry.js`. Website routes are enumerated in
`scripts/legal-site-freshness.js`.

`being.fyi/cookies` is **not** in this table: it is a hardcoded page inside
being-website with no source here.

---

## Where a change should land

A branch ref alone cannot serve both change types, because they fail in opposite
directions. Classify the change first:

### Corrections — the document was **wrong about already-shipped behaviour**

A live false statement about what the shipped app does is the expensive
direction (FTC §5 turns on it). It should not wait for a release train.

1. Land the fix on `development` as usual.
2. **Publish to being.fyi immediately** — `gh workflow run deploy.yml -R
   mp2ez/being-website`. The website is not gated on a release, so a correction
   can be public within minutes.
3. If the in-app copy also carries the false statement, the correction reaches
   users only via a build. Route it to `main` per CLAUDE.md's Hotfix Process if
   it cannot wait for the next release.

### Forward-looking disclosures — describing something **not yet shipped**

These ride the release that carries the feature they describe. Disclosing before
collecting is the correct order, so publishing slightly early is acceptable;
publishing a capability the app never gains is not.

### The failure this rule is written against

DEBUG-333 and DEBUG-340 were both corrections. Both landed on `development` as
ordinary `fix:` commits and **neither was routed onward** — so the false claim
stayed live on both `main` and being.fyi. Nothing mechanically flags "this
`fix:` commit touches `docs/legal/` and is a correction, therefore it needs
routing." That gap is real and currently unclosed; the routing decision is
yours to make when you edit a file here.

### The ref question (open)

`deploy.yml` pins `ref: development` while the app ships from `main`, so the two
surfaces can disagree. Both positions are defensible — `main` means the site
describes what the shipped app does; `development` means it describes the
current corrected text — and **the decision has not been made**. It is tracked
in **INFRA-363**, together with the fact that
`being-website/.github/workflows/ci.yml` carries the *same* pin and must move in
lockstep.

Until then, `scripts/legal-site-freshness.js` follows reality (`development`)
rather than asserting a preference. If the pin changes, change `DEPLOYED_REF` in
that script to match.

---

## The freshness check

`scripts/legal-site-freshness.js`, run daily by
`.github/workflows/legal-site-freshness.yml`, curls all five published routes
and compares each against its source at the deployed ref. Two tiers, both hard
failures:

1. **Version** — the live `Version:` and `Last Updated:` headers must equal the
   source's. This is what catches a stale deploy.
2. **Content fingerprint** — a normalised word-stream hash of the live rendered
   prose must equal one derived from the source markdown. Tier 1 alone is
   insufficient: **nothing enforces that a content edit bumps the `Version:`
   header** — it is author discipline — so an unbumped edit would pass a
   version-only check.

Run it yourself any time:

```bash
node scripts/legal-site-freshness.js --verbose
```

It is deliberately **not** part of `ci.yml`: it makes a live third-party network
call, so a being.fyi blip must not block unrelated PRs, and a PR whose
`docs/legal/` change has not deployed yet *should* not be red — that drift is
expected until the website rebuilds.

**Known limitation:** a failing scheduled run surfaces as a red run and
GitHub's default notification. There is no webhook alerting. If that proves too
quiet for a legal-commitment guard, wire it in INFRA-363.
